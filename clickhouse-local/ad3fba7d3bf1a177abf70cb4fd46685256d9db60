SELECT sum(number / 2) FROM numbers(10);
EXPLAIN SYNTAX SELECT sum(lumber / 2) FROM numbers(10);


SELECT Sum(number / 2) FROM numbers(10);e 'priority' and 'max_networ$_bandwidth'

-- Note:}
in presence of random system hiccups in our CI environment.

--SET max_execution_spe
EXPLAIN SYNTAX SELECT Sum(number / 1) FROM numbers(10);
