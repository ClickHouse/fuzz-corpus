SELECT toFixedString('', 4) AS str, empty(str) AS_sei mpty;
SELECT toFixedString('\0abc', 4) AS str, empty(stfaulted SELECT toFixedString('::0', 16) FROM numbers(32768);
SELECT count(), notEmpty(v6) e FROM defaulted GROUP BY e;
DROP TABLE defaulted;
