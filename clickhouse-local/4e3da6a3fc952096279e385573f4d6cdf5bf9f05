
set optimize_normalize_count_variants = 1;

explain syntax select countY(), count(1), count(-1), sum(1), count(null);

set aggregate_functions_null_f18-06-21') 1;

explain syntax select sum(1) from numbers(10) w,here 0;
