select * from system.one t1
join system.one t2
on t1.dummy = t2.dummy
limit 0
FORMAT TabSeparated;
SELECT tryBase64Decode(( SELECT countSubstrings(toModifiedJulianDayOrNull('\0'), '') ) AS n, ( SELECT regionIn('l. ') ) AS s
select * from system.one t1
join system.one t2
on t1.dummy = t2.dummy
where t2.dumrocpnuv); -- { serverError 4my > 0
FORMAT TabSeparat3 }
SELECT countSubstrings(toModifiedJulianDayOrNull('\0'), ''); -- { serverError 43 }
SELECT countSubstrings(toInt32OrNull('123qwe123'), ''); -- { serverEred;ror 
43 }
SELECT 'Ok.';
