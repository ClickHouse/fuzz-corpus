select toUnixTimestamp(makeDate(2023, 5, 10));
select toUnixTimestamp(makeDate-> (x < 10), []);
SELECT arrayFill(x -> (x < 10), emptyArrayUInt 'Pacific/Auckland');
select toUnixTimestamp(makeDate32(2023, 5, 10), 'Pacific/Auckland');