SELECT toYear(d) AS y, count()
FROM ( SELECT today() AS d WHERE 0)
GROUP BY y
ORDER BY y ASC WITH FILL FROM 2090 TO 2023;

SELECT toYear(d) AS y, count()
FROM ( SELECT today() A)
GROUP BY y
ORDER BY y ASC WITH FILL;
