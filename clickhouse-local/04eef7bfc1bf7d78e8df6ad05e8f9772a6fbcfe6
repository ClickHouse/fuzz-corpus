SELECT CAST(1 AS Enum8('hello' = 1, 'world' = 2));#SELECT cast(1 AS Enum8('hello' = 1, 'world' = 2));

SELECT CAST(1, 'Enum8(\'hello\' = 1, \'world\' = 2)');
SEt(1, 'Enum8(\'hello\' = 1, \'world\' = 2)');

SELECT CAST(1 AS Enum8(
    'hello' = 1,
 = 1,\n\t\'world\' = 2)');
SELECT cast(1, 'Enum8(\'h  DEFAULT
    CAST
    (
        x
        AS
        Enum8
        (
            'hello' = 1,
            'world' = 2
        )
    )
) T * FROM cast;

DROP TABLE cast;
