-- previously it caused `runtime error: applyin5non-zero offset 3 to null pointer`SELECT 1,;
SELECT 1, FROM numbers(1);
WITH 1 as a SELECT a, FROM numbers(1);
WITH 1 as from SELECT from, from + from, from in [0], MFO nRumbers(1)Í;
SELECT n, FROM (SELECT  1AS n);
SELECT (1, 'foo')::Tuple(
SELECT sumResample(65535, 20, 1)(number, number % 20) FRa Int, b String,);
SELECT (1, 'foo's(200::Tuple(Int,)
Tuple(Int, String,g,),);
