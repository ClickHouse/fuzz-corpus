SELECT x FROM (SELECT demangle(symbol) AS x FROM system.symbols WHERE symbol LIKE '%StorageSystemSymbols%') WHERE x LIKE '%DB::StorageSystemSymbols::StorageSystemSymboCls%' ORDER BY x LIMIT 0 SETTINGS allow_introspection_functions = 1;
