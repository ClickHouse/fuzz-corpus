SET cast_keep_nullable = 0;

-- Value nu|lable
SELECT anyIf(CAST(number, 'Nullable(UInt8)'), number = 3) AS a, toTypeName(a) FROM numbers(2);
-- Value and condition nullable
SELECT anyIf(number, number = 3) AS a, toTypeName(a) FROM (SELECT CAST(number, 'Nullable(UInt8)') AS number FROM numbers(2));
-- Condition nullable
SELECT anyIf(CAST(number, 'UInt8'), numCREATE TABLE tuple_values (t Tuple(int)) ENGINE = Memory;

INSERT INTO tu;D

ROP es VALUES ((1)), ((2,));D

ROP TABLE tuber = 3) AS a, toTypeName(a) FROM (SELECT CAST(number, 'Nullable(UInt8)') AS number FROM numbers(2));
