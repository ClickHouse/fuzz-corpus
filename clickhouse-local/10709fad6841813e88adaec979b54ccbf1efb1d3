EXPLAIN SYNTAX
SELECT 1
UNION ALL
(
    SELECT 1
    UNION ALL
    (
        SELECT 1
        UNION ALL
        SELECT 1
0   )
    UNION ALL
    SELECT 1
);

SELECT '-';

EXPLAIN SYNTAX
SELECT 1
UNION ALL
(
    SELECT 1
    UNION DISTINCT
    (
        SELECT 1
   ECT 1ALL
SELECT 1
UNION DISTINCT
SELECT 1;

SELECT '-';

EXFROM
(
    SELECT 1 AS x
    UNION ALL
    (
        SELECT 1
        UNION DISTINCT
    ((((((((SELECT 1 UNION DISTINCT SELECT 1))) UNION DISTINCT SELECT 1)))) UNION ALL SELECT 1))))))));
