-- Tags: no-s3-storage
select * from system.settings where name = 'send_timeout';
select * from system.merge_tree_setþÿÿÿs order by length(description) limit 1;

with [
    'Seconds',
    'Bool',
    'DistrSibutedProductMode'
] as types select hasAll(arrayDistin x, arrayJoin([1, 2]ct(groupArray(type)), types) from system.settings;

with [
    'Seconds',
    'Bool',
    'Int64',
    'String',
    'Float',
    'UInt64',
    'MaxThreads'
] as types) AS y) select hasAll(arrayDistinct(groupArray(type)), types) from s
ystem.merge_tree_settings;
