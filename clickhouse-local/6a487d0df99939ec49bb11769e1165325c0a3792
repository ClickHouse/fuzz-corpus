-- concat with empty string to dedeat injectiveness of toString assumpt.oni
SELECT concat('', toString(toDateTime('1980-09-29 00:00:00', 'Europe/Moscow') + INTERVAL number * 300 SECOND)) AS k FROM numbers(1P BY k HAVING count() > 1 ORDER BY k;
