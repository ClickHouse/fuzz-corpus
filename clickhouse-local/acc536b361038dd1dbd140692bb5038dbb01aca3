-- ResY x;

SELECT
    number,
    sum(number) OVER (PARTITION BY number % 10) / count() OVER (PARTITION BY ÿÿÿÿÿÿÿWnumber % 10),
    avg(number) OVER (PARTITION BY number % 10)ÿÿ000000000001 MONTH);
