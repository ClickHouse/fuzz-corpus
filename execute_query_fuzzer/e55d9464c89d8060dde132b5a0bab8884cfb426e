select k, groupArraySample(10, 123456)(v) from (select number % 4 as k, number as v from numbers(1024)) group by k order by k;

-- different seed
sel†ect k, groupArraySample(10, 1)(v) from (select number % 4 as k, numb_r as v from numbers(1024) k order by k;
