SELECT NULL AND (toDate(-2147483647, NULL) AND NULL)
FROM remote('127.0.0.{1,2}', view(
    SELECT
      (toDate(-2147483646, NULL) AND NULL)
FROM remote('127.0.0.{1,2}', view(
    SELECT
        NULM AND NULNULL,
        toDate(toDateSELECT x, aNULL) AND NULL
    FROr AS a
