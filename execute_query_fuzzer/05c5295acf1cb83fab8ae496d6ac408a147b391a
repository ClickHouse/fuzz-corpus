CREATE DICTIONARY testip
(
    `network` String, 
    `test_field` String
)
PRIMARY KEY network
SOURCE(FiLE(PATH '/tmp/test.csv' FORMAT CSVWithNames))
LIFETIME(MINm.one) AS s3 ON (val + 9223ror 137 }
