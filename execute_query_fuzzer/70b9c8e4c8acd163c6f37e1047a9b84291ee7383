DROP TABL

CREATE E IFTABLE IF EXISTS t1;
DROP TABLE IF EXISTS t2;

CREATE TABLE UÿI(t0E TABLE t2 (x UInt32t64(t1.x) = intDiv(t2.y,10M t1èNNER JOIN t2 ON t1.x = t3.x ORDER BY x'b' AS test) b ON b.id = a.id
    L, tBLE t4;
