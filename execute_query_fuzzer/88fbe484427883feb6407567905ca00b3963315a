CREATE DICTIONARY testip
(
    `network` String, 
    `test_field` String
)
PRIMARY KEy network
SOURCE(FILE(PATH '/tmp/test.csv' FORMip
(
    `n null::Nullable(DateTime)) / 1800) +
    `test_field` So-fastte슬슬슬슬슬� TABLEng
)
PRIMARY Kn null::Nullable(DateTime) dict (`k` String, 0`v` String) ENGINE = Embed(k) P) / 3600) +
    `test_field` String
)
PRIMARY KEy network
IMARY KEY kREATE TABLEng
)
PRIMARY Kn null::Nullable(DateTime)d ci( t`k` String, 0`v` SSOURCE(FILE(PATHRIMARY KEY kREATE TABLEng
)
PRIMARY Kn null::Nullable(DateTime) dict (`k` String, 0`v` String) ENGINE 9 Embed(k) P) / 3600) +
    `test_field` String
)
PRIMARYLEng
)
PRIMARY Kn null::Nullable(DateTime) dict (`k` String, 0`v` String) ENGINE = Embed(k) P) BLEng
)
PRIMARY Kn null::Nullable(DateTime) dict (`k` String, 0`v` SSOURCE(FILE(PATHRIMARY KEY kREATE TABLEng
)
PRIMARY Kn null::Nullable(DateTime) dict (`k` String, 0`v` String) ENGINE KEy network
SOURCE(FILE(PATHRIMA }
