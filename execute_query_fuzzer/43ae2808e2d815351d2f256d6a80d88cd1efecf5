SELECT *
FROM
(
    WITH

   (
    WITH

        (
      (
    WITH

   (
    WITH
SEL
     SELECT 1 AS a, 2 AS b FROM systECT (0.1::Decimal(4, 4) + 0.2::Decimal(-- Tags: no-)replicat  */
    09e6, /*  80e6  /
 max_bytes  */
);

SET max_memory_us)ags  */
);

SET max_memory_usage=10e6;4, 4) + 0.3::Decimaecimal(4, BY a, b;
