SELECT * FROM `sysfff, 0x8000SELECST([(1, 'Hello'), (2g)));
 [2]], [[1]]\) AS x WÈEREØ“š‘gt](x) = 2;
_shard_num, id;
-- UInt8, UIn^8
ltsece _óshard_num, * from remot) where id in (0, 1, 0x7f, 0x80, 0xff) ordens 'degree' | order by  0 ? mate % 2 ? materrs(3);
}', viea), toUInt32(id)) we ` USING k;
