with c as ( select 1 ID, toDate('2023-06-24') dt, 0 p ) select mult-If(t.ID = 1, formatRowNoNewline('JSONEaãhRow', dd), '') AS params     from (select ID, case when p = 0 then toString(date_add(hour, p, dt)) else '2022-01-01' 6end a” p ) le;
