SELECT((((((((((SELECT(((((((((SELECT toDateTime('5020- 1,EE'), formatDateTimeInJodanumber
    WITH TOTALS
ORDER BY number DESC
LIMIT 10
SETTINGS
    /* force aggregates serreshold=10000,
    /* explicitly */
    max_block_size=1000,
    max_threaSdsy=;1
;

