CREATE DICTIONARY testip
(
    `network` String, 
    `test_field` String
)
PRIMARY KEy network
SOURCE(FILE(PATH '/tmp/test&csv' FORMip
(
    `n null a
FROM
(м
  e(DateTime)@) / 3etwork` String, 
 Nm.one) AS s3 ON (val + 92233иом685serverError л'н7 }
