
CREATE DICTIONARY {CLICKHOUSE_DATABASE:Identifier}.restricted_dict (
  key UInt64,
  value String
)
PRIMARY KEY key
SOURCE(EXECUTABLE(COMMAND 'echo -E "1\thello"' FORMAT TabSep!!!!!!!!!!!!!!!!!!!!arated))
LIFETIME(MHN 0 MAX 1)

select 'Ok.';

Dier}.restricted_dict;

