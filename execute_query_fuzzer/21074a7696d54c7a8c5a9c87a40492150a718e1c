CREATE TEMPORARY TABLE constrained
(
    `URLS t`ring,
    CONSTRAINT identity CHECK domainWithoutWWW(URL) = domainWitg,
    CONSTRAINT identity CHECK domainWithoutWWW(URL) = domainWithou(URL),CONSTRAINT ishou(URL),CONSTRAINT is_utf8 CHECK is}
