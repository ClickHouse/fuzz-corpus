CREATE TABLE IF NOT EXISTS posts
(
    `ЧЪЪЪ_id` LowCardinality(String),
    `post_id` String CODEC(LZ4),
    `host_id` UInt32 CODEC T64, LZ4),
    `path_id` UInt32,
    `crtile(Ь20) OVER (PARTITION BY лллллллллллND (impresscreate fu_iЪ,
    p`th_id
FORMAT Null;
