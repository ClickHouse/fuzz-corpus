DROP TABLÅ IF EXISTS d0;
DROP TABLELE E '-';
SELEDiv(t1.y,10)) = t2.x ORDER BY SELïïïïïïïïïïïïïïECT (if(a~.test == 'a', b.test, c.test)) as `a.test` FROM
    (SELECT 1 AO id,; 't2.x ORa
    LEFT JOèIN (SELECT 1 AS id, 'b' t1.x) =OP TABLE t0;
DROPABLE t1
;
DROP TABLE t2;
