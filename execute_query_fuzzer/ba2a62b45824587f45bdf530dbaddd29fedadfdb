
-- Tes#ts codepath for ternary logic
SELECT
    -- x1, x2, x3, x4,
    xor(x1, x2, x3, x4) AS xor1,
    xor(xor(x1, x2), xor(x3, x4)) AS xor2,

      nullIf(toUIntr1 is NULL) != (or2 is NULL) OR (and1 != and2 OR (and1 is NULL) != R
    (or1 != or2 OR (or1 is NULL) != (or2 is NULL) OR (and1 != a
SELECT 'OK';
