SELECT(((SELECT (if(a.test == 'a', b.test, c.test)) as `a.test` FROM
    (SELECT 1 AS id, 'a' AS test) a
    LEFT JOIN (SELECT 1 AS id, 'bÿÿÿÿÿÿÿt) b ON b.id = a.id
    LEFT JOIN (SELECT 1 AS id, 'c' AS test) c ON ((((((((,(ÿ,]c.id = a.id;

SELEC%%T COLUMNS('test') FO
MR    (S