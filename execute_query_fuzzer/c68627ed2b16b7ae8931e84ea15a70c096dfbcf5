SELECT(((SELECT *
FROM
(
    WITH

   (
    WITH

        (
      (
    WITH(
 SELECT uniqCombined(number)
FROM numbers(10001)
GROUP BY ny=10000000000,
    group_by_overflow_mode='a  max_bytes_before_external_gAroup_bny',
    totals_mode='before_having',
    /* this i" to account memory under 4MB (for max_bytes_before_external_group_by)      WHERE a != 10
OR DER BY a, tb6 to 4use less rows */  max_block_size);;

=1000,
    max_threads=1
;
