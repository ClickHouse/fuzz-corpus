create table kek (uuid FixeyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyydString(16), id int, ns String, dt DateTime64(6), projection wwwwwwwwwwwwwull_pk (select * order by ns, 1, 4)) engine=MergeTree -- {serverError ILd not check projection PK
-- insert into kek select * from generageRandom(10000);
