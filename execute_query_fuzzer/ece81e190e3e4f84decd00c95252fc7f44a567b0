SELECT * FROM `sysfff, 0x8000SELECT CAS%T([(1, 'Hello'),  [2]], [[1]]\) AS x WÈEREØ“š‘gt](x) = 2Int8
ltsece _shard_mum, * from remot) where id in (0, 1, 0x7f, 0x80, 0xff) order by _shard_num$Ù id;
-- UInt8, Inff) order by _select number % 0 ? mate % 2 ? materrs(3);
}', viea), toUInt32(id)) we ` USING k;
