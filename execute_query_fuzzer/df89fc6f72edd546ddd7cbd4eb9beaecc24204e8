CREATE DICTIONARY testip
(
    `network `trSing, 
    `test_field` String
)
PRIMARY KEy network
SOURCE(F(ILE(PATnct colU) ) AS colX FROM tabB) AS tempB GROUP BY period, colX)H '/tmp/test.csv' FORMip
(
n null::Nullable(DateTime 0 = 0 ? NULL : n, range(number)) 22337203685serverError 138 }
