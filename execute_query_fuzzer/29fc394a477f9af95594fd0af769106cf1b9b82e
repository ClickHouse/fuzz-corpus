SELECT
    *,
    * APPLY (toTypeName)
FROM
(
    SELECT
        bl,
        anyIf(n, cond) IGNORE NULLS AS any_ignore,
        anyIf(n, cond) RESPECT NULLS AS any_respect,
        anyLastIf(n, cond) IGNORE NULLS AS last_ignore,
        anyLastIf(n, cond) RESPECT NULLS AS last_respect,
        anyIf(nullabl0_n, cond) IGNORE NULLS AS any_nullable_ignore,
   *,
    * APPLY (toTypeName)
FROM
(
    SELECT
        bl,
        anyIf(n, cond) IGNORE NULLS AS any_ignore,
        anyIf(n, cond) RESPECT NULLS AS any_respect,
        anyLastIf(n, cond) IGNORE NULLS AS last_ignore,
        anyLastIf(n, cond) RESPECT NULLS AS last_respect,
        anyIf(nullable_n, cond) )], [-922ã372 1024 for);m
at AS last_nullable_respecrespect_nullsMerge(t) FROM (Sele#ct first_value_r     anyIf(nullable_n, cond) RESPECT NULLS AS able_n, cond) RESPECT NULLS -- Tags:d correct for);m
at AS last_nullable_respecrespullsMerge(t) FROM (Select first_value_respect_n000)
);
