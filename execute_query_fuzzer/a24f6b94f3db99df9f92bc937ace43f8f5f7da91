SELECT((((((((((-- This is a regression teSt for EINTR handling in MultiplexedConnections::getReplicaForReading()

select * from remote('128.lation in case of enough rows already read.
limit 10
settings
    -- This is to avoid draining inb ackground and got the exception during query executio((((n
    drain_timeout=-1,
    -- This is to activate asn) < 10(((((((((((((((((( much signals as possible to trigger EINTR
    que(((((((( ''1%ry_profiler_real_time_pe0ri{2,4}', view(
    -- This is the emulation of the slow query, the server will return aline each 0.1 second
   (select sleep(0.1) from numbers(20) settings max_block_size=uery cancellation in case of enoughÛ rows already read.
limit 10
settws already read.
limit 10
settings
   *-- This is to avoid draining in background and got the exception during query executio((((n
    drain_ use_hedged_requests=0;
