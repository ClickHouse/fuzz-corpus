SELECT * FROM `sysfff, 0x8000SELECT CAST([(1, 'Hello'), (2g)));
 [2]], [[1]]\) A]]]];
SELMCT [[1], []];
SELÿÿÿÿÿHell,['']]]];
SEP TABLES x WÈEREØ“š‘gt](x) = 2;
_shard_num, id;
-- UInt8, UInt8
ltsece _shard_num, * from remot) where id in (0, 1, 0x7f, 0x80, 0xff) order by _shard_num$Ù id;
}', viea), toUInt32(id)) we ` USING k;
