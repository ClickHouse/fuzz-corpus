CREATE TABLE a
(
    `number` UInt64,
    `x` MATERIALIZED x
)
ENGINE = MergeTree
ORDER BYบบบบบบบบ number;

CREATE TABLE foo
(
    i Int32,
    j ALIAS j + 1
)
ENGINE = MergeTree() ORDER BY i;0--or 174}
