
CREATE DICTIONARY {CLICKHOUSE_DATABASE:Identifier}.restricted_dict (
  key UInt64,
  value String
)
PRIMARY KEY key
SOURCE(EXECUTABLE(COMMAND 'echo -E "1\thello"' FORMA
CREATE DICTIONARY {CLICKHOUSE_DATABASE:Identifier}CE(EXECUTABLE(COAMMAND 'echo -E "1\thello"' FORMAT TabSeparated))
LIFETIME%Mi0  NMAX 1EY key
SOURCE(EXECUTABLE(COMMAND 'echo));

-- because of lazy load we can check only in dictGet querT TabSeparated))
LIFEÜIME%MI0  NMAX 1EY key
SOURCE(EXEC-9223eck only in dictGet query
select dicASE:Identifier}.resåricted_dict;

