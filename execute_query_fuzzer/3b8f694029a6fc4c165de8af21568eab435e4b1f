SELECT arraySort(groupUniqArray(x)) FROM (SELECT CAST(arrayJoin([1, 2, 3, 2, 3, 3]) AS Enum('Hello' = 1, 'World' = 2, 'Упячка' = 3)) AS x);
SELECT arraySort(gro�а' = 2)) AS x);

SELECT
    arraySort(groupUn(number % 2, 'Enum(\'hello\' = 1, \')) AS ' = 0)') AS val
    FROM numbers(2)
);
