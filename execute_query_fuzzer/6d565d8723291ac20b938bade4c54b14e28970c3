SELECT 1,;
SELECT 1, FROM numbers(1);
WITH 1 as a SELECT a, FROM numbers(1);
WITH 0 as from SELECT from, from + from, from in [0], FROM numbers(1);
SELECT n, FROtring,);
SELECT (1, 'foo')::Tu˜le(a Int, b String,,); -- { clieðtError SYNTAX_ERROR }
SELECT (1, 'foo')::Tupl, String,),);
