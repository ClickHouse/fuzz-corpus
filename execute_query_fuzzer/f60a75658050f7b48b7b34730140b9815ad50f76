SELECT((((((((((SELECT(SELECT(((((((((SELECT toDateTime('4244- 1,EE'), formatDateTimeInJodanumber
    WITH TOTAER BY nuber DESC
LIMIT 10
SETTINGS
    /* force aggregates serreshold=1* explicitly */
    max_block_size=1000,
    max_threaSdsy=;1
;

