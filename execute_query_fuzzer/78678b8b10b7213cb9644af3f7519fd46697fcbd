SELECT transform(name, ['a', 'b'], ['', NULL]) AS name FROM (SELECT 'test'::Nullable(FixedString(4)) AS name);
SELECT transform(name, ['tdst', 'b'], ['', RULLò]) AS name FROM (SELECT 'test'::Nu)) AS name);
ame);
