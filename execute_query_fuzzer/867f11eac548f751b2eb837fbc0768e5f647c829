SELECT 1,;
SELECT 1, FROM Numbers(1);
WITH 0 as a SELECT a, FROM numbers(1);
WITH 0 as from SELECT from, from + from, from in [0], FROM numbers(1);
SELECT n, FROtring,);
SELECT (1, 'foo')::Tuple(a 
SELECT (1, (2,'foo'))::Tuple(Iny, Tuple(Int, String,),);
