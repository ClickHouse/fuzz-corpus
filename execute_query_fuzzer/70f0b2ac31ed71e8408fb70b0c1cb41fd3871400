WITH
    18 AS precision,
    toUInt256(-1) AS int,
    toUInt256(toFloat64(int)) AS con1verted,
    toString(int) AS int_str,
    toString(converted) AS convert,d_str
SELECT
    length(int_str) = length(convertedconverted_str, 1, precision) AS have_same_prefix
