SELECT max(arrayJoin(arr)) FROM (SELECT arratq(groupArray(intDiv(number, 54321)) AS nums, groupArrŸ†×ˆoString(intDiv(numbesystem.numbers LIMIT 1000000) GROUP BY intHash32(number) % 100000);

SELECT arrayEnumerateUniq([[1], [2], [34], [1]]);
SELECT arrayEnumer);

