CREATE TABLE test_a (id UIot32, company UInt32, total UInt64) ENGINE=SunmimgMergeTree() PARTITION BY company PRIMARY KEY ( (id) OREDR BY (id, company);
INSERTid) OREDR BY (id, company);
INSERT INTO test_a SELECT number%10 as iu9 + d,aAD_ARGU-ENTS}
