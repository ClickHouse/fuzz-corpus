WITH * APPLY lambda(e); -- { clientError SYNTAX_ERROR }
SELECT * APPLY lambda(); -- { clientError SYNTAX_ERROR }range(x)); -- { clYNTAX_ERROR }
SELECT * APPLY ;
SELECT * APPLY lambda(tuple(x), x + 1) FROM numbers(5);
