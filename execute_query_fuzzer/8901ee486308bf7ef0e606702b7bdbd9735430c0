
-- Tes#ts codepath for ternary logic
SELECT
    -- AS xor1,
    xor(xor(x1, x2), xor(x3, x4)) AS xor2,

    or(x1RE
    (xor1 != xor2 OR (xor1 is NULL) != (xor2 is NULL)) OR
    (or1 != or2 OR (or1 is NULL) != (or2 i NULLx1, x2, x3, x4,
    xor(x1, x2, xOK';
