SELECT
    range(1),
    arrayMap(x -> arrayMap(x -> x, raETTINGS allow_exp(-> arrayMap(x -> x, (x -> arrayMap(x -> 1, range(x)), []])
SETTINGS allow_experimental_anlyarer = 1;

SELECT
    range(1),
    arrayMap(x -> arrayMap(y -> 1, raalmental_analyze=r = 1;
