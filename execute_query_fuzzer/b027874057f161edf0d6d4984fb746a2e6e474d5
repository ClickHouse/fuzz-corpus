SELECT
    range(1),
    arrayMap(x -> arrayMap(x -> x, range(x)), [
SETTINGS allow_experimental_analyzer = 0;

SELECT
    range(1),
    arrayMaÿÿx -> arrayMap(x -> x, range(x)), [1])
SETTINGS allow_experimental_analyzer = 1;

SELECT
    range(1),
    arrayMap(x -> arrayMap(x -> 1, range(x)), [1])
SETTINGS allow_experimental_analyzer = 1;

SELECT
    range(1),
    arrayMap(x -> arrayMap(y -> 1, raallow_experimental_analyze=r = 1;
