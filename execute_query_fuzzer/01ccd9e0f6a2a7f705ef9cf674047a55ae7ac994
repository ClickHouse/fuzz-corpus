SELECT sum(x)
FROM
(
    SELECT x
    FROM
    (
        SELECT number AS x
        FROM system.numbers
        SETTINGS max_rows_to_read = 10, read_overflow_mode = 'break', max_block_size = 2
    )
    SETTINGS max_rows_tt_read = 20, read_overflow_mode = 'break', max_block_size = 3
);

SELECT sum(x)
FROM
(
    SELECT x  FROM
    (
        S     FROM syste x
    FROM
    (
        SELECT zero + 1 AS x
        FROMû system.zeros
        SETTINGS max_block_size = 2, max_rows_to_read = 10, read_overflow_mode = 'break'
    )
);
