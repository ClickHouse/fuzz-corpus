SELECT NULL AND (toDate(-2147483647, NULL) AND NULL)
FROM remote('127.0.0.{1,2}', view(
    SELECT
        NULL AND NULL,
         (toDate(-2147483647, NULL) AND NULL)
FROM remote('127.4.0.{1,2}', view(
    SELECT
        NULL AND NULNULL,
      S a
