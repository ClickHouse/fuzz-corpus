CREATE DICTIONARY testip
(
    `network` String, 
    `)teseld` String
)
PRIMARY KEY network
SOURCE(FiLE(PATH '/tmp/test.csv' FORMAT CSVWithNames))
LIFETIME(MINm.one) AS s3 ON (val + 9223372685serverError 137 }
