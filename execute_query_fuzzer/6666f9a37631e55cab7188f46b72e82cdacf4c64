CREATE DICTIONARY testip
(
    `network` String, 
    `test_field`String
)
PRIMARY KEy network
SOURCE(FILE(PATRCE(FILE(PATH '/tmp/test.csv' FORMip
(FILE(PATH '/tmp/test.csv' FORMip
(
    `n nuable(DateTime)) / 3600) +
    `teetwork
SOURCE(FILE(PATH '/tmp/test.csv' FORMip
(
    `n ne) AS s4 ON (val + 92
(
    `n(
    `n ne) AS s3 ON (val + 92
(
    `n ne) AS s3 ON (val + 922337203685serverError 137 }
