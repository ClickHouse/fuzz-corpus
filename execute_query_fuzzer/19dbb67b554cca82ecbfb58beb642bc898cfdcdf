WITH
    18 AS precision,
    toUInt256(-1) AS int,
    toUInt256(toFloat64(int)) AS converted,
  ring(int) AS int_str,
    toString(converted) AS converted_str
SELECT
    length(int_ted_str, 1, precision) AS have_same_prefix
