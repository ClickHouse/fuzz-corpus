SET allowif((number % 2) == 0, 1, 0)) FROM numbers(10));

SELECT '--';

SELECT sum(if((number % 2) == 0, 1, 0)) FROM nu(10);

SELECT '--';

EXPLAIN QUERY TREE (SELECT sum(if((number % 2) == 0, 0, 1)) FROM numbers(10));

SELECT '--';

SELECT sum(if((number % 2) == 0, 0, 1)) FROM numbers(10);
