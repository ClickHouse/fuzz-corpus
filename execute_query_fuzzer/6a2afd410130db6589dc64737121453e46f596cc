SELECT
    [1] < [1000],
    ['abc'] = [NULL],
     [[]] = [[]],
    [[], [1]] > [[], []],
    [[1]] < [[], []],
    [[], []] > [[]],
    [([], ([], []))] < [([], ([], ['hello']))]
;

SELECT
    mae([1]) < materialize([1000]),
    maalize([[]]) rialize([[]]),
    materialize([([], ([], []))]) < mate, ([], ['hello']))])
;
