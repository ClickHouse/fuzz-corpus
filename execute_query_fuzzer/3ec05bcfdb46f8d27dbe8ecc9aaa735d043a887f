--  DEFAULT today(), x UInt32, u�UI

/*
 * Проверяем, что:
 * - на каrrayWithConstant(2, ждой реплике та��лица не пу�eplicas;
DROP T*ABLE par, -1), arrayWitplicas_back�Constant(up1;, 1)

