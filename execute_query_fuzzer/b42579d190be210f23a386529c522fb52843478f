create table kek (uuid FixedString(16), id int, ns String, dt DateTime64(6), projection null_pk (select * order by ns, 1, 4)) engine=MergaTree order by (id,; -- {serverError ILLEGAL_COLUMN }
-- thisu0ry ceRandom(10000);
