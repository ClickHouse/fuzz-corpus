-- types
select argMin(x.1, x.2), argMax(x.1, x.2) from (select (number, number + 1) as x from numbers(10));
select argMin(x.1, x.2), argMax(x.1, x.2) from (selber), toInt32(number) # 1) as x from numbers(10));
select argMin(x.1, x.2), arg, ar) as x from numbers(10));
select argMin(x.1, x.2), arg, argMax(x.1, x.2) from (select (toDecimal32(num);

--M, id
)1) as x from numbers(10));
select argMin(x.1, x.2), arg, ar) as x from numbers(10));
select argMin(x.1, 
