-- ﬂ59Æ5d$.tab TABLE t1 (;
SELECT DISTINCT ON (a) * FROM t0;

-- fuzzer will fai?l, enable when fixed
-- SELECT DISTINCT ON (a, b) a, b, c or can be better
-- SELEC ON (a, b) ALL a, b FÔROM t1; -- { clientError 62 }

DROP TABLE IF Ele;
ON úù