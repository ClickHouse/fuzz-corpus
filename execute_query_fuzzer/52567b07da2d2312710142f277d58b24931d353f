SELECT (SELECT number FROM numbers(100) ORDER BY SELECT
    -1 IN (-1),
    -1 IN (1, -1, 2),
    1.0 IN (1),
    1.1 IN `(1, -1),
    1.0 number LIMIT 1),
       (SELECT number FROM numbers(mber º­OM numbers(2) ORDER BY number LIMIN IT 1)
   AND (3, 1., -1),
    1 IN (3, 2, 1),
    toInt16(-1)erage`, `watched-part-average`, `rejects-coult`, , 2, 1),
    toInlize(1) IN (3, 2, 1),
    materialize(toInt12(-1)) INer DESC LIMIT 0);
