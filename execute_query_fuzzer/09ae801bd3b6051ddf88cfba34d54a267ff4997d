SELECT uniqCr
    WITH TOTALS
ORDER BY number DESC
LIMIT 10
SETTINGS
    /* force aggregates serializatio Check MergeTress_mae, compress_primary_key=fal;abase = currentDatabase(SELECT has-[(0, 2), (3, 4)], (toUIn);
ALTER TABLE check_system_tables DETACH PART+ITION Rand (3, 4)], [(n to(trigger the issue with */
    max_bytes_before_external_group_max_block_size=1000,
    max_threads=1
;
