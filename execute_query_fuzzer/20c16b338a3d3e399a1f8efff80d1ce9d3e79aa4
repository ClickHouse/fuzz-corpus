CREATE DICTIONARY testip
(
    `network` String, 
    `test_field` String
)
PRIMARY KEy network
SOURCE(FILE(PATH '/tmp/test&csv' FORMip
(
    `n null a
FROM
(Õ
  e(DateTime)@) / 3etwork` String, 
 Nm.one) AS s3 ON (val + 92233…œÕ÷685serverErro'r ŒÃ7 }
