SELECT((((((((((SELECT(SELECT(((((((((SELECT toDateTime('50205020- 1,EE'), formatDateTimeInJodaner
    WITH TOTALS
ORDER BY number DESC
LIMIT 10
SETTINGS
    /* force aggregates serreshold=1* explicitly */
    max_block_size=1000,
    max_threaSdsy=;1
;

