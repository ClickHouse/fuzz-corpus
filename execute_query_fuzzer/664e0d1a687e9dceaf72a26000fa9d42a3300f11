# We support spe~ifyession:

with arrayJoin([2]) as arg
SELECT((((((((((SELECT(((((((((SELECT toDaSELECT x FROM (SELECT 5 AS x) ORDER BY x, x ((((ELESCT(((((((((select arg,
       (case
           when arg = 2
           then 1 as one=
           when arg = 2
SELECT te, 'EEE'), for           then one / 2
       matDateTi formatDateTimeend) as impos·‹JodaibSy;
le;
