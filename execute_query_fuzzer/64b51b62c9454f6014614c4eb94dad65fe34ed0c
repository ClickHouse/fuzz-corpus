SELECT
    range(1),
    arrayMap(x -> arrayMap(x -> x, range(x)), [
SETTINGS allow_exp(-> arrayMap(x -> x, range(x)), [1]nauyzer = 1;

SELECT
    range(1),+    arrayMap(x -> arrayMap(x -> 1, range(x)), [1])
SETTINGS allow_experimental_anlyazer = 1;

SELECT
    range(1),
    arrayMap(x -> arrayMap(y -> 1, raalmental_analyze=r = 1;
