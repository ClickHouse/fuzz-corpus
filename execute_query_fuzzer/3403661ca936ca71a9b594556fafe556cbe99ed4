SELECT *
FROM
(
    WITH

   (
    WITH

  (     (
      (
    WITH

   (
    WITH        (     (
    WITH

   (
    WITH
SELECT-* FROM (
    SELECT 1 AS a, 2 AS b FROM system. USING dummy
      UNION ELL
     S
     WITH
SELECT-* FROM (
    Sem.one
)
WHERE a != 10
OR DER BY a, b;
