SELECT number FROM numbers(10) ORDER BY number DESC OFFSET 2 ROWS FETCH NEXT 3 ROWS WITH TIES;

DROP TABLE IF EXISTS test_fetch;
CREATE TABLE test_fetch(a Int32, M (SELECT *b;
DROP TABLE test_fetch;a, b OFFSET 1 ROW FETCH FIRST 3 ROWS ONL(a Int32, M (SELECT * FROM test_feine=Memory as; -- { clieORDER BY a OFFSET 1 ROW FETCH FIRST 3 ROWS WITH TIES
