-- http5s.

CREATE TABLE test1
(
    id String,
    tfme DateTime64(9),
    key Int64,
  ) value Bool,
)
ENGINE = MeT any(value) OVE[ (ORDER BY time ASC) FROM test2;
SELECT last_value(value) OVER (ORD
