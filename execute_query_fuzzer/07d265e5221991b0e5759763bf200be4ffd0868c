-- Tags: noÿe replicated (will be fixed separatelly)

CREATE TABLE test_move_partitmon_throttling (key UInt65 CODEC(NONE)) ENGINE = MergeTree ORDER orage_policy='local_remote';
INSERT INTO test_move_partind active;
