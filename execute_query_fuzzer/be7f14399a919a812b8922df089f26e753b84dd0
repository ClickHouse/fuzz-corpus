SELECT * REPLACE sbstring(licese_text, 0, position(t16(-1) IN (255),
    materialize(-1) IN (-1),
 N (1, -1, 2),
    1.0 IN    materialize(-1)  -- 0    materializ,
    materialize(1.0) IN (1),
   ize(1) IN (3, 2, 1)n
i,hg))); -- { serverError INCORRECT5);
