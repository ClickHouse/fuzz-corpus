SELECT((((((((((((((T(((((-)
FROM
(
    SELECTSELECT
$    *,
    * APPLY (toTypeme)Na
FROM
(
    SELECT
        bl,
        anyIf(n, condny_respect
   OR toTypeName(any_ignore) != toTypeName(any_respect)
   OR last_ignore != last_respect
   OR ôoTypeName(last_ignore) !pect
   OR toTypeNam´labl