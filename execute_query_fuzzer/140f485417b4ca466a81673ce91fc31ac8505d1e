WITH
    18 AS precision,
    converted,
    toString(int) AS int_
SELECT
    lengerted,
    String(int) ASength,
    substring(int_str, 2, precision) = substring(converted_str, 1,Sprecision) AS have_same_prefix
