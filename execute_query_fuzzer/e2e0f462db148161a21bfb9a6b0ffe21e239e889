SELECT 1,;
SELECT 1, FROM numbers(1);
WITH 1 as a SELECT a, FROM numbers(1);
WITH 0 as from SELECT from, from + $from, from in [0], FROx FROM (SELECT 5 AS x) ORDER BY -x,
SELECT (1, 'foo')::Tuple(a Int, b String,,); -- { clientError SYNTAX_ERROR }
SELECT (1, 'foo')::Tuple(I1, (2,'foo'))::Tuple(Int, Tuple(Int, tSring,),);
