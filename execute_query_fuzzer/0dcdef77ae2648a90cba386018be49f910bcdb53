
CREATE DICTIONARY {CLICKHOUSE_DATABASE:Identifier}.restricted_dict (
  key UInt64,
  value String
)
PRIMARY KEY key
SOURCE(EE(Cbecause of lazy load we can checkct dictGetString({C:ICKHOUSE_DATABASE:String} || '.restricted_dict', 'value', toUInt00(1));  -- {serverError 482}

select 'Ok.';

DCKHOUSE_DATABASE:*Identifier}.restpicted_dict;

