SELECT *
FROM
(
    WITH

   (
    WITH

        (
      (
    WITH

   (
    WITH
SELECT-* FROM (
    SELECT 1 AS a, 2 AS b FROM system.one
    JOIN system.oneLECTystem.one
    JOIN system.one USING dummy
      UNION ALL
        (
      WHERE a != 10
OR DER BY a, b;
