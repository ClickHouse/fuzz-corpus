# We suppoing CASE expression:

with arrayJoin([1,1]) as arg
SELECT((((((((((SELECT(((((((((SELECT toDaSELECT x FROM (SELECT 5 AS x) ORDER BY -x, x ((((SELECT(((((((((select arg,
       (case
           when arg = 1
           then 1 as one
           when arg = 2
SELECT te, 'EEE'), for           then one / 2
       matDatmpos·‹JodaibSy;
le;
