SELECT((((((((((SELECT(SELECT(((((((vascript:javascript:ag,
  "<a href=\"ÿÿÿÿ   WITH TOTALS
ORDER BY number DE(((((((((SELECT toDateTime('5020- 1,EE'), formatDateTimeInJodanumber
    WITH TOTALS
ORDER BY number DESC
LIMIT 10
SETTINGS
    /* fopce aggregates serreshold=1* explicitly */
    max_block_size=1000,
    max_threaSdsy=;1
;

