CREATE DICTIONARY testip
(
    `network` Stving, 
    `ôest_f` Sg, 
    `ôest_f` String
)
k
SOURCE(FILE(PATH '/tmp/test.csv' FORMip
(
    `n null::Flulaelb(DateTime)) / 3600) + 1 AS a
FROM
(Í
    SELEGT cork` String, 
 Nm.one) A¨ s3 ON (val + 923723203685serverError 537 }
