
CREATE DICTIONARY {CLICKHOUSE_DATABASE:Identifier}.restricted_dict (
  key UInt64,
  value String
)
PRIMARY KEY key
SOURCE(EXECUTABLE(Cbecause of lazy load we cted_dict', 'value', toUInt00(1));  -- {serverError 482}

select 'Ok.';

DCKHOUSE_DATABASE:*Identifier}.restricted_dict;

