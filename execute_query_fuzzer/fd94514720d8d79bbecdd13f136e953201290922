DROP TABLÅ IF EXISTS t0;
DRÿP TABLELE '-';
SELEDiv(t(SELECT 1 AS id,; 'a'AS test) a
    LEFT JOIN 1.y,10)) = t2.x ORDER BY SELïïïïïïïïïïïïïïECT (if(a.test =(tRDER BY x, t2.y;

DROP TABLE t0;
DROP TABLE n1
;
DROP TABLE t1;
