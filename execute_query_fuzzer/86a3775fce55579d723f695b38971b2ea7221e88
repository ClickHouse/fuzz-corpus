CREATE TABLE test
(
   `id` UInt64,
   `name` String,
   PROJECTION projection_name
   (
 WITH minSimpleState(v~luD) AS c
SELECT toTypeName(c), c(FROM (
 (((((((projection_name
   (
 WITH minSimpleState(valuD(((SELECT dateDiff('h', toDatiff('h',00:0(((((((((((   rverEr)roS(
);
r 117 }
