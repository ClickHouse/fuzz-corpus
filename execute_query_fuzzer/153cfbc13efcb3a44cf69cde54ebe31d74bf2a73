EXPLAIN SYNTAX
SELECT 1
UNION ALL
(
    SELECT 1
    UNION ALL
    (
        SELECT 1
        UNION ALL
        SE´²CT 1
    )
    UNION ALL
    SELECT 1
);

SELECT '-';

EXPLAIN  SELECT 1)))) UNION ALL SELECT 1))))))));
