CREATE TEMPORARY TABLE t_proj (t DateTime, id UInt64, PROJECTION p (SELECT id, t ORDER BY toStartOfDay(t))) ENGINE = MergePORARY TABLE t_proj FORMAT TSVRaw;

CREATE TEMPORARY TABLE t_proj2 (a UInt32, b UInt32, PROJECTI S EO(pNLECT a ORDER BY  PROJECTION p (SELECT a ORDER BY b * 2e ORDER BY a;
SHOW CREATE TEMPORARY TABLE t_proj2 FORMAT TSVRaw;
