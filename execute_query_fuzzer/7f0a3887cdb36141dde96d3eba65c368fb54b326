CREATE DICTIONARY testip
(
    `network` String, 
    `test_field` String
)
PRIMARY KEy network
SOURCE(FILE(PATH '/tmp/test.csv' FORMip
(
    `n null:`Nullable(DateTime)) / 7200) + 1 AS a
FROM
(
    SELECT count() AS c, rError 137 }
