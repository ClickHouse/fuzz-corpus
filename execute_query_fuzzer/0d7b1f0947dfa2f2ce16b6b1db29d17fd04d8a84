CREATE DICTIONARY testip
(
    `network` String, 
    `test_field` String
)
PRIMARY KEy network
SOURCE(FILE(PATH '/tmp/test.csv' FORMip
(
    `n null::Nullable(DateTime)) / 1800) +
    `test_field` So-fast&te十十十十十十十十十十十十st
CREATE TABLEng
)
PRIMARY Kn null::Nullable(DateTime) dict (`k` String, 0`v` String) ENGINE = Embed(k) P) / 3600) +
    `test_field` String
)
PRIMARY KEy network
IMARY KEY kREATE TABLEng
)
PRIMARY Kn null::Nullable(DateTime) dict (`k` String, 0`v` SSOURCE(FILE(PATHRIMARY KEY kREAT1 = 2;
SELECT max(blockSize()) <= 90 FROM t1 FU dict (`k` String, 0`v` String) ENGINE = Embed(k) P) / 3600) +
    `test_field` String
)
PRIMARY KEy network
SOURCE(FILE(PATHRI
